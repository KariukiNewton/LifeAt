function Spaces({ onClose }) {
    return (
        <div className="spacesContent">
            <h2>Spaces Page</h2>
            <p>This is the Spaces content.</p>
            <button onClick={onClose}>Close</button>
        </div>
    );
}

export default Spaces