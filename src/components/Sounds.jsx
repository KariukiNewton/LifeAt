function Sounds({ onClose }) {
    return (
        <div className="soundsContent">
            <h2>Sounds</h2>
            <p>This is the Sounds content.</p>
            <button onClick={onClose}>Close</button>
        </div>
    );
}

export default Sounds